## 🔄 Listando Valores

Use `for...in` para percorrer o objeto `carro` e adicione todos os **valores** em um array chamado `valores`.

**Template**

```javascript
const carro = { marca: "Ford", modelo: "Ka", ano: 2020 };

// 12. Valores
let valores = [];
```

<details>
  <summary>🫣 Spoiler</summary>

> `for (let chave in carro) { valores.push(carro[chave]); }`

</details>
