## 🔄 Listando Chaves

Use `for...in` para percorrer o objeto `carro` e adicione todas as **chaves** (nomes das propriedades) em um array chamado `chaves`.

**Template**

```javascript
const carro = { marca: "Ford", modelo: "Ka", ano: 2020 };

// 11. Chaves
let chaves = [];
```

<details>
  <summary>🫣 Spoiler</summary>

> `for (let chave in carro) { chaves.push(chave); }`

</details>
