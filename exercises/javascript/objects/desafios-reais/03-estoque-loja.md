## 🌍 Estoque de Loja

Dado um objeto `estoque` onde a chave é o produto e o valor é a quantidade, crie uma função `verificarEstoque(produto)` que retorna "Disponível" se > 0 ou "Esgotado".

**Template**

```javascript
// 18. Estoque
const estoque = { notebook: 5, mouse: 0 };
function verificarEstoque(produto) {}
```

<details>
  <summary>🫣 Spoiler</summary>

> `return estoque[produto] > 0 ? ... : ...`

</details>
