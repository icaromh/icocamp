// --- Exercício 6 ---
let cachorro = {
  nome: "Rex",
  // Implemente latir aqui
};

// --- Verificação ---
console.log(
  "6. Latir:",
  cachorro.latir && cachorro.latir() === "Au Au!" ? "✅" : "❌"
);
