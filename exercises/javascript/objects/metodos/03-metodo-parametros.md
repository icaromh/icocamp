## ⚙️ Método com Parâmetros

Crie um objeto `calculadora` com um método `somar(a, b)` que retorna a soma de `a` e `b`.

**Template**

```javascript
// 8. Calculadora
let calculadora = {
  // ...
};
```

<details>
  <summary>🫣 Spoiler</summary>

> `somar: function(a, b) { return a + b; }`

</details>
